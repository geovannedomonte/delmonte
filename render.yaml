services:
  - type: web
    name: delmonte-pizzaria
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PAGBANK_TOKEN
        sync: false
      - key: PAGBANK_ENV
        value: sandbox
      - key: WEBHOOK_URL
        sync: false